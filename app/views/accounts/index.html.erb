<h2><strong>All ACCOUNT</strong></h2>
	<div class = "container">
		<% @accounts.each do |acc| %>
		<div class="row jumbotron col-md-12">
			<div class = "col-md-6" >
					<h3><strong><%= link_to acc.account_name, account_path(acc.id) %></strong></h3>
					<p class="card-text">Welcome to
                                <%=acc.account_name%> team.</p>
                    </div>
                    <div class=" col-md-4">
                            <a href="<%=account_path(acc.id)%>" class="btn btn-success" style = " float: right">See Team</a>
					</div>
			</div>
		<%end%>
	</div>