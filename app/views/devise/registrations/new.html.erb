<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :check_account_id,:value => params[:acco_id]  %>
  <%= f.hidden_field :check_user_email,:value => params[:emaill]  %> 
  <%debugger%>
  <div class="container jumbotron">
   
    <div class="row d-flex justify-content-center">  
      <div class="col-md-8 ">
      <%if params[:emaill].present? %> 
        <div class="form-group">
          <h2>Sign up</h2>
      
          <%= f.label :email %><br />
          <%= f.email_field :email, :value => params[:emaill], autofocus: true,:readonly => true, autocomplete: "email", class: "form-control" %>
          <!-- text_field :ecard, :sender, :value => 'contact@host.com' -->
        </div>
      <% else %> 
        <div class="form-group"> 
          <h2>Sign up</h2>
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>
          
            <div class="form-group">
              <%= f.label :account_name %><br />
              <%= f.text_field :account_name, autofocus: true, autocomplete: "off", class: "form-control" %>
            </div>
          
        <%end%>
         <div class="form-group">
          <%= f.label :name %><br />
          <%= f.text_field :name,  autofocus: true, autocomplete: "off", class: "form-control" %>
          <!-- text_field :ecard, :sender, :value => 'contact@host.com' -->
        </div>
        <div class="form-group">
          <%= f.label :password %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
        </div>
        
          
        <div class="actions">
          <%= f.submit "Sign up" %>
        </div>
        
      </div>
    </div>
  </div>



<% end %>


